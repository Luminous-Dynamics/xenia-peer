name: Ledger Persistence Outcome

on:
  pull_request:
    paths:
      - 'crates/xenia-ledger/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/ledger-persistence-outcome.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ledger-persistence-outcome-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  source:
    name: Outcome-aware persistence qualification
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install reviewed Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy,rustfmt

      - name: Format xenia-ledger
        run: cargo fmt --check --package xenia-ledger

      - name: Test xenia-ledger
        run: cargo test --locked -p xenia-ledger

      - name: Clippy xenia-ledger
        run: cargo clippy --locked -p xenia-ledger --all-targets -- -D warnings

      - name: Record exact-head evidence
        if: always()
        run: |
          E="$RUNNER_TEMP/ledger-persistence-evidence"
          mkdir -p "$E"
          git rev-parse HEAD > "$E/HEAD"
          git rev-parse HEAD^{tree} > "$E/TREE"
          rustc -Vv > "$E/RUSTC.txt"
          cargo -V > "$E/CARGO.txt"
          sha256sum Cargo.lock > "$E/CARGO_LOCK_SHA256.txt"
          sha256sum crates/xenia-ledger/src/chain.rs > "$E/OUTCOME_AWARE_CHAIN_SHA256.txt"
          sha256sum crates/xenia-ledger/src/durable_frontier.rs > "$E/DURABLE_FRONTIER_SHA256.txt"

      - name: Upload source evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ledger-persistence-outcome-${{ github.sha }}
          path: ${{ runner.temp }}/ledger-persistence-evidence
          if-no-files-found: error
          retention-days: 14
