name: Operation Store Frontier Contract

on:
  pull_request:
    paths:
      - "contracts/xenia-operation-store-frontier/**"
      - ".github/workflows/operation-store-frontier.yml"
  push:
    branches: [main]
    paths:
      - "contracts/xenia-operation-store-frontier/**"
      - ".github/workflows/operation-store-frontier.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    name: Format + Test + Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt,clippy
      - name: Format
        run: cargo fmt --manifest-path contracts/xenia-operation-store-frontier/Cargo.toml -- --check
      - name: Test
        run: cargo test --manifest-path contracts/xenia-operation-store-frontier/Cargo.toml
      - name: Clippy
        run: cargo clippy --manifest-path contracts/xenia-operation-store-frontier/Cargo.toml --all-targets -- -D warnings

  msrv:
    name: MSRV Rust 1.94
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.94
      - name: Check
        run: cargo check --manifest-path contracts/xenia-operation-store-frontier/Cargo.toml --all-targets
