name: Xenia Symthaea Authorization Receipt

on:
  pull_request:
    paths:
      - "crates/xenia-authority-symthaea-authorization-receipt/**"
      - ".github/workflows/authority-symthaea-authorization-receipt.yml"
  push:
    branches: [main]
    paths:
      - "crates/xenia-authority-symthaea-authorization-receipt/**"
      - ".github/workflows/authority-symthaea-authorization-receipt.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: symthaea-authorization-receipt-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    name: Symthaea daemon authorization receipt contract
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: crates/xenia-authority-symthaea-authorization-receipt

    steps:
      - uses: actions/checkout@v4

      - name: Install pinned Rust 1.94
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.94.0
          components: rustfmt, clippy

      - name: Format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Frozen authorization receipt vectors
        run: cargo test --all-features
