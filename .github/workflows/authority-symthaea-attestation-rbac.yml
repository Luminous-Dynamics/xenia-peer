name: Xenia Symthaea Attestation RBAC

on:
  pull_request:
    paths:
      - "crates/xenia-authority-symthaea-attestation-contract/**"
      - "crates/xenia-authority-symthaea-attestation-verifier/**"
      - "crates/xenia-authority-symthaea-attestation-authority/**"
      - "crates/xenia-authority-symthaea-rbac/**"
      - ".github/workflows/authority-symthaea-attestation-rbac.yml"
  push:
    branches: [main]
    paths:
      - "crates/xenia-authority-symthaea-attestation-contract/**"
      - "crates/xenia-authority-symthaea-attestation-verifier/**"
      - "crates/xenia-authority-symthaea-attestation-authority/**"
      - "crates/xenia-authority-symthaea-rbac/**"
      - ".github/workflows/authority-symthaea-attestation-rbac.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: symthaea-attestation-rbac-${{ github.ref }}
  cancel-in-progress: true

jobs:
  policy:
    name: Symthaea attestation role policy
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: crates/xenia-authority-symthaea-rbac

    steps:
      - uses: actions/checkout@v4

      - name: Install pinned Rust 1.94
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.94.0
          components: rustfmt, clippy

      - name: Format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Test fail-closed RBAC mapping
        run: cargo test --all-features
