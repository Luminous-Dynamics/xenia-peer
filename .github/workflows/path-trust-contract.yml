name: Xenia Path Trust Contract

on:
  pull_request:
    paths:
      - "contracts/xenia-path-trust/**"
      - ".github/workflows/path-trust-contract.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt,clippy
      - name: Resolve and identify standalone dependency lock
        working-directory: contracts/xenia-path-trust
        run: |
          cargo generate-lockfile
          sha256sum Cargo.lock
      - name: Format
        working-directory: contracts/xenia-path-trust
        run: cargo fmt --check
      - name: Test
        working-directory: contracts/xenia-path-trust
        run: cargo test --locked
      - name: Clippy
        working-directory: contracts/xenia-path-trust
        run: cargo clippy --locked --all-targets -- -D warnings

  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.94
      - name: Resolve standalone dependency lock
        working-directory: contracts/xenia-path-trust
        run: cargo generate-lockfile
      - name: Rust 1.94 check
        working-directory: contracts/xenia-path-trust
        run: cargo check --locked --all-targets
