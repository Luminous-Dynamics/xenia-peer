name: Operation Global Revocation Transition

on:
  pull_request:
    paths:
      - "crates/xenia-ledger/**"
      - "contracts/xenia-operation-authority-epoch/**"
      - "contracts/xenia-operation-store-frontier/**"
      - "contracts/xenia-operation-frontier-ledger-witness/**"
      - "experimental/xenia-operation-frontier-ledger-adapter/**"
      - "experimental/xenia-operation-frontier-retention-bundle/**"
      - "experimental/xenia-operation-frontier-governed-transition/**"
      - "experimental/xenia-operation-global-revocation-transition/**"
      - "docs/ADR-022-operation-frontier-ledger-witness.md"
      - "docs/ADR-023-operation-frontier-external-retention.md"
      - "docs/ADR-025-governed-operation-frontier-transitions.md"
      - "docs/ADR-026-global-operation-authority-revocation.md"
      - ".github/workflows/operation-global-revocation-transition.yml"
  push:
    branches:
      - "feat/operation-global-revocation-transition-v1"
      - main
    paths:
      - "crates/xenia-ledger/**"
      - "contracts/xenia-operation-authority-epoch/**"
      - "contracts/xenia-operation-store-frontier/**"
      - "contracts/xenia-operation-frontier-ledger-witness/**"
      - "experimental/xenia-operation-frontier-ledger-adapter/**"
      - "experimental/xenia-operation-frontier-retention-bundle/**"
      - "experimental/xenia-operation-frontier-governed-transition/**"
      - "experimental/xenia-operation-global-revocation-transition/**"
      - "docs/ADR-022-operation-frontier-ledger-witness.md"
      - "docs/ADR-023-operation-frontier-external-retention.md"
      - "docs/ADR-025-governed-operation-frontier-transitions.md"
      - "docs/ADR-026-global-operation-authority-revocation.md"
      - ".github/workflows/operation-global-revocation-transition.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  revocation:
    name: Live + Historical Revocation Composition
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt,clippy
      - name: Resolve standalone lock
        working-directory: experimental/xenia-operation-global-revocation-transition
        run: cargo generate-lockfile
      - name: Record complete authority dependency topology
        working-directory: experimental/xenia-operation-global-revocation-transition
        run: |
          cargo tree -p xenia-operation-global-revocation-transition
          cargo tree -p xenia-operation-frontier-governed-transition
          cargo tree -p xenia-operation-frontier-retention-bundle
          cargo tree -p xenia-operation-authority-epoch
          cargo tree -p xenia-ledger
      - name: Format
        run: cargo fmt --manifest-path experimental/xenia-operation-global-revocation-transition/Cargo.toml -- --check
      - name: Test
        run: cargo test --manifest-path experimental/xenia-operation-global-revocation-transition/Cargo.toml --locked
      - name: Strict Clippy
        run: cargo clippy --manifest-path experimental/xenia-operation-global-revocation-transition/Cargo.toml --locked --all-targets -- -D warnings

  msrv:
    name: Global Revocation MSRV Rust 1.94
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.94
      - name: Resolve standalone lock
        working-directory: experimental/xenia-operation-global-revocation-transition
        run: cargo generate-lockfile
      - name: Check complete dependency stack
        run: cargo check --manifest-path experimental/xenia-operation-global-revocation-transition/Cargo.toml --locked --all-targets
