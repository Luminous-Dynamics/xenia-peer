# PQ Signature Fixtures

This directory is reserved for PQ signature known-answer vectors.

No vectors are active yet.

`full-pqc-v1` must remain refused until this directory contains reviewed vectors and the Rust harness verifies them through a real backend.
