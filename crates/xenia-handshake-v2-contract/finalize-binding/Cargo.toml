[package]
name = "xenia-handshake-v2-finalize-binding-contract"
version = "0.0.0-m0"
edition = "2024"
rust-version = "1.94"
authors = ["Tristan Stoltz <<maintainer-email>>"]
license = "Apache-2.0 OR MIT"
description = "Fail-closed HostFinalizeV2 V5 binding contract for Xenia"
repository = "https://github.com/Luminous-Dynamics/xenia-peer"
publish = false

[dependencies]
thiserror = "1"
xenia-handshake-v2-contract = { path = ".." }

[dev-dependencies]
xenia-negotiation = { path = "../../xenia-negotiation" }
xenia-negotiation-codec = { path = "../../xenia-negotiation-codec" }

# Staged independently from both the parent contract workspace and production
# until the finalize-binding regression is qualified and deliberately folded
# into the live V2 handshake implementation.
[workspace]
