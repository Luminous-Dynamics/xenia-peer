[package]
name = "xenia-sif-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license = "AGPL-3.0-or-later"
description = "Application-layer runtime typestates joining Xenia SIF protocol verification to protected filesystem custody."
repository.workspace = true
homepage.workspace = true
publish = false

[dependencies]
xenia-ledger = { path = "../xenia-ledger" }
xenia-peer-core = { path = "../xenia-peer-core" }
thiserror = "2.0"

[dev-dependencies]
blake3 = "1.5"
uuid = { version = "1", features = ["serde"] }

[lints]
workspace = true
