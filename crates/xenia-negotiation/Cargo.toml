[package]
name = "xenia-negotiation"
version = "0.0.0-m0"
edition = "2024"
rust-version = "1.94"
authors = ["Tristan Stoltz <<maintainer-email>>"]
license = "Apache-2.0 OR MIT"
description = "Canonical authenticated-handshake capability context primitives for Xenia"
repository = "https://github.com/Luminous-Dynamics/xenia-peer"
homepage = "https://github.com/Luminous-Dynamics/xenia-peer"
publish = false

[dependencies]
sha2 = "0.10"
thiserror = "1"

# Keep this preparatory semantic primitive outside the production root workspace
# until V2 handshake integration consumes it deliberately.
[workspace]
