[package]
name = "xenia-negotiation-policy"
version = "0.0.0-m0"
edition = "2024"
rust-version = "1.94"
authors = ["Tristan Stoltz <<maintainer-email>>"]
license = "Apache-2.0 OR MIT"
description = "Canonical local minimum-capability policy for Xenia negotiation"
repository = "https://github.com/Luminous-Dynamics/xenia-peer"
publish = false

[dependencies]
sha2 = "0.10"
thiserror = "1"
xenia-negotiation = { path = "../xenia-negotiation" }

[dev-dependencies]
hex = "0.4"

# Keep this preparatory policy crate outside the production root workspace until
# the V2 handshake consumes negotiation policy evidence explicitly.
[workspace]
